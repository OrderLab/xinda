* `./razor-scripts` contains all the source code we need to run the experiments
* `./xinda-software` contains some softwares we need to get the distributed systems (i.e., Cassandra, HBase and HDFS) running
